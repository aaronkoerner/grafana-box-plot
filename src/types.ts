export interface SimpleOptions {
  text: string;
  showSeriesCount: boolean;
  seriesOrientation: string;
  lineWidth: number;
  numTicks: number;
  autoscale: boolean;
  minScale: number;
  maxScale: number;
  boxColor: string;
  tickColor: string;
  whiskerColor: string;
  medianColor: string;
  outlierColor: string;
  gridLines: boolean;
}
